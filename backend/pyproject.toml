[project]
name = "clara-backend"
version = "0.1.0"
description = "Clara AI Chat Backend - FastAPI + ADK"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "google-adk",
    "python-dotenv",
    "python-multipart",
    "pydantic",
]

[tool.uv]
package = false
